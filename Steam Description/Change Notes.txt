Will be moved to steam every release.

major:
tighter path checking for asteroids (still working on performance cost)

minor:
settings now have minimum and maximum values
Put those cores to work! Can run multiple path checking tasks in parallel, see Settings.
fixed turrets not firing at blocks
fixed reset when target block / grid is destroyed
